spack:
  concretization: together
  specs:
  - clingo-bootstrap@spack
  - patchelf
  - py-boto3
  - python ~dbm ~libxml2
  view: ~/.setup-spack/view
  config:
    install_tree:
      root: ~/.setup-spack/opt
    modules:
      enable: []
  packages:
    all:
      target:
      - x86_64